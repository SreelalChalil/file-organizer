# File Organizer - UI

This is a React frontend that talks to the backend REST API at /api/*.

To run locally:

```bash
cd file-organizer/ui
npm install
npm run dev
```

To build:

```bash
npm run build
```
